# Wallpapers are applied over IPC by ~/.config/hypr/set-wallpaper.sh, which
# picks a random image from ~/github/wallpapers on every Hyprland start.
# Nothing is preloaded statically here on purpose.
ipc = on
splash = false
